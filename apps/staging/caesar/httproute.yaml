apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: caesar-route
spec:
  parentRefs:
    - name: traefik-gateway
      namespace: traefik
  hostnames:
    - "caesar.staging.mercury.local"
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: caesar-n8n
          port: 80
